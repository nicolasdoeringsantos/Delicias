{
  "name": "delicias-da-nossa-casa",
  "version": "1.0.0",
  "private": true,
  "description": "Site da confeitaria Delícias da Nossa Casa",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.21.0",
    "wrangler": "^4.121.0",
    "vite": "^7.1.7"
  }
}
